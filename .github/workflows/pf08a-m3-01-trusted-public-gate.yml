name: PF-08A M3-01 Trusted Public Gate

on:
  pull_request:
    branches:
      - main
    paths:
      - tools/pf08a-m3-01-public-browser-smoke.mjs
      - docs/execution-batches/PF-08A-M3-01-PLAN-OBLIGATIONS/Public_Verification.md
      - docs/execution-batches/PF-08A-M3-01-PLAN-OBLIGATIONS/Batch_Manifest.md
      - docs/execution-batches/PF-08A-M3-01-PLAN-OBLIGATIONS/Checkpoint_Ledger.md

permissions:
  contents: write

concurrency:
  group: pf08a-m3-01-public-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  verify-public-package:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Verify exact checked-out head
        shell: bash
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"

      - name: Verify published Plan and obligations package
        shell: bash
        env:
          PUBLIC_URL: https://maloma.github.io/sandbox/
          EXPECTED_MAIN: d2be5fe98dc44d635f30c857659fcad562fe54c4
        run: |
          evidence=docs/execution-batches/PF-08A-M3-01-PLAN-OBLIGATIONS/Public_Verification.md
          if test -f "$evidence"; then
            node tools/pf08a-m3-01-public-browser-smoke.mjs
            grep -Fq '**Status:** PASS' "$evidence"
            grep -Fq 'd2be5fe98dc44d635f30c857659fcad562fe54c4' "$evidence"
          else
            mkdir -p "$(dirname "$evidence")"
            node tools/pf08a-m3-01-public-browser-smoke.mjs --evidence="$evidence"
          fi

      - name: Persist public verification evidence
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          evidence=docs/execution-batches/PF-08A-M3-01-PLAN-OBLIGATIONS/Public_Verification.md
          if git diff --quiet -- "$evidence"; then
            echo "Public M3 evidence is already committed."
            exit 0
          fi
          encoded_path=$(python -c 'import sys,urllib.parse; print(urllib.parse.quote(sys.argv[1], safe="/"))' "$evidence")
          content=$(base64 -w0 "$evidence")
          payload=$(jq -n --arg message "Record M3 public verification" --arg content "$content" --arg branch "$HEAD_BRANCH" '{message:$message,content:$content,branch:$branch}')
          curl --fail-with-body -sS -X PUT \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/contents/${encoded_path}" \
            -d "$payload"
