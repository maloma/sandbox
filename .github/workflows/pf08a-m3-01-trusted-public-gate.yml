name: PF-08A FamilyPilot Trusted Public Gate

on:
  pull_request:
    branches:
      - main
    paths:
      - tools/pf08a-m4-01-public-browser-smoke.mjs
      - docs/execution-batches/PF-08A-M4-01-SAVINGS-GOALS/Public_Verification.md
      - docs/execution-batches/PF-08A-M4-01-SAVINGS-GOALS/Batch_Manifest.md
      - docs/execution-batches/PF-08A-M4-01-SAVINGS-GOALS/Checkpoint_Ledger.md
      - .github/workflows/pf08a-m3-01-trusted-public-gate.yml

permissions:
  contents: write

concurrency:
  group: pf08a-m4-01-public-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  verify-public-package:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Verify exact checked-out head
        shell: bash
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"

      - name: Verify published M4-01 FamilyPilot package
        shell: bash
        env:
          PUBLIC_URL: https://maloma.github.io/sandbox/
          EXPECTED_MAIN_M4_01: f7366c0229fdc5115e38b5ea34e4c498a09e73f3
        run: |
          evidence=docs/execution-batches/PF-08A-M4-01-SAVINGS-GOALS/Public_Verification.md
          if test -f "$evidence"; then
            node tools/pf08a-m4-01-public-browser-smoke.mjs
            grep -Fq '**Status:** PASS' "$evidence"
            grep -Fq 'f7366c0229fdc5115e38b5ea34e4c498a09e73f3' "$evidence"
          else
            mkdir -p "$(dirname "$evidence")"
            node tools/pf08a-m4-01-public-browser-smoke.mjs --evidence="$evidence"
          fi

      - name: Persist public verification evidence
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          evidence=docs/execution-batches/PF-08A-M4-01-SAVINGS-GOALS/Public_Verification.md
          if git ls-files --error-unmatch "$evidence" >/dev/null 2>&1 && git diff --quiet -- "$evidence"; then
            echo "Public M4-01 evidence is already committed."
            exit 0
          fi
          encoded_path=$(python -c 'import sys,urllib.parse; print(urllib.parse.quote(sys.argv[1], safe="/"))' "$evidence")
          content=$(base64 -w0 "$evidence")
          payload=$(jq -n --arg message "Record M4-01 public verification" --arg content "$content" --arg branch "$HEAD_BRANCH" '{message:$message,content:$content,branch:$branch}')
          curl --fail-with-body -sS -X PUT \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/contents/${encoded_path}" \
            -d "$payload"