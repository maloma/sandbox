(()=>{
  'use strict';
  if(window.__FP_M4_06_SOLVER_UI_BOOTSTRAP__)return;
  window.__FP_M4_06_SOLVER_UI_BOOTSTRAP__=true;
  const deadline=Date.now()+40000;
  const wait=()=>{const runtime=window.__FP_RUNTIME__,solver=window.FamilyPilotWhatIfSolver,whatIf=window.FamilyPilotWhatIf,budget=window.FamilyPilotBudgetDesigner,accounts=window.FamilyPilotSavingsAccounts;if(!runtime||!solver||!whatIf||!budget||!accounts||window.__FP_M4_06_READY__!==true){if(Date.now()>=deadline){window.__FP_M4_06_SOLVER_UI_ERROR__='M4-06 solver dependencies did not become ready';return}setTimeout(wait,25);return}install(runtime,solver,whatIf,budget,accounts)};
  function install(runtime,solver,whatIf,budget,accounts){
    if(window.__FP_M4_06_SOLVER_READY__)return;
    const{state,$,esc,money,now}=runtime,deps={solver,whatIf,budget,accounts,scope:runtime.scopeApi};
    let mode='deficit',source='actual',scenarioId='',horizonDays=30,goalId='',targetDate='',incomeShare=50,lastResult=null;
    const fmtDate=value=>value?new Intl.DateTimeFormat('ru-RU',{day:'numeric',month:'short',year:'numeric'}).format(new Date(value)):'—';
    const activeGoals=()=> (state.savingsGoals||[]).filter(item=>item.status==='active');
    const scenarios=()=>whatIf.activeScenarios(state);
    const marker=document.createElement('meta');marker.name='familypilot-package';marker.content='m4-06-goal-deficit-solver-v1';document.head.appendChild(marker);
    const style=document.createElement('style');style.id='familypilot-m4-06-solver-style';style.textContent=`
      .m406-solver-tabs{display:grid;grid-template-columns:1fr 1fr;gap:7px}.m406-solver-tabs button{border:1px solid var(--line);border-radius:12px;background:var(--card2);color:var(--muted);padding:9px;font-weight:900;font-size:10px}.m406-solver-tabs button.active{background:var(--blue);border-color:var(--blue);color:white}.m406-solver-result{display:grid;gap:8px;margin-top:10px}.m406-solver-callout{border:1px solid var(--line);border-radius:14px;padding:10px;background:var(--card2);font-size:11px;line-height:1.45}.m406-solver-callout strong{display:block;margin-bottom:3px}.m406-solver-split{display:grid;grid-template-columns:1fr 1fr;gap:8px}.m406-solver-range{display:grid;grid-template-columns:1fr auto;gap:8px;align-items:center}.m406-solver-range input{width:100%}.m406-solver-limit{color:var(--orange);font-size:10px;margin-top:5px}@media(max-width:390px){.m406-solver-split{grid-template-columns:1fr}}
    `;document.head.appendChild(style);
    function installDom(){const comparison=$('m406Comparison'),screen=$('whatIfScreen');if(screen&&!$('m406Solver')){const section=document.createElement('section');section.id='m406Solver';section.className='card section';comparison?.parentNode?comparison.parentNode.insertBefore(section,comparison):screen.appendChild(section)}}
    function sourceOptions(){return`<option value="actual"${source==='actual'?' selected':''}>Текущий реальный план</option>${scenarios().map(item=>`<option value="scenario:${esc(item.id)}"${source==='scenario'&&scenarioId===item.id?' selected':''}>Сценарий: ${esc(item.name)}</option>`).join('')}`}
    function goalOptions(){const items=activeGoals();if(!goalId||!items.some(item=>item.id===goalId))goalId=items[0]?.id||'';return items.map(item=>`<option value="${esc(item.id)}"${item.id===goalId?' selected':''}>${esc(item.name)}</option>`).join('')}
    function defaultTarget(){if(targetDate)return targetDate;const date=new Date();date.setMonth(date.getMonth()+6);targetDate=date.toISOString().slice(0,10);return targetDate}
    function renderResult(){if(!lastResult)return'<div class="m406-empty">Выберите параметры и нажмите «Рассчитать».</div>';if(!lastResult.ok)return`<div class="error">${esc(lastResult.error)}</div>`;if(lastResult.kind==='deficit'){const split=lastResult.split;return`<div class="m406-solver-result"><div class="m406-grid"><div class="m406-metric"><span>Текущий минимум</span><strong class="${lastResult.initialMinimumOperating<0?'m406-negative':''}">${money(lastResult.initialMinimumOperating,'EUR')}</strong></div><div class="m406-metric"><span>Дыра в бюджете</span><strong>${money(lastResult.deficitAmount,'EUR')}</strong></div><div class="m406-metric"><span>Изменение в месяц</span><strong>${money(lastResult.requiredMonthlyRelief,'EUR')}</strong></div><div class="m406-metric"><span>Минимум после расчёта</span><strong class="${lastResult.resultingMinimumOperating>=0?'m406-positive':'m406-negative'}">${money(lastResult.resultingMinimumOperating,'EUR')}</strong></div></div><div class="m406-solver-split"><div class="m406-solver-callout"><strong>Необходимый дополнительный доход</strong>${money(split.additionalIncome,'EUR')} в месяц<br><small>${esc(lastResult.incomeMeaning)}</small></div><div class="m406-solver-callout"><strong>Необходимое сокращение расходов</strong>${money(split.expenseReduction,'EUR')} в месяц<br><small>${esc(lastResult.expenseMeaning)}</small></div></div><div class="m406-note">Критическая дата: <strong>${fmtDate(lastResult.criticalDate)}</strong>. В модели использовано ежемесячных точек: ${lastResult.modeledReliefEvents}. Ничего не добавлено в план и не применено автоматически.</div></div>`}const split=lastResult.split;return`<div class="m406-solver-result"><div class="m406-grid"><div class="m406-metric"><span>Осталось накопить</span><strong>${money(lastResult.remainingAmount,'EUR')}</strong></div><div class="m406-metric"><span>До выбранной даты</span><strong>${lastResult.monthsAvailable} мес.</strong></div><div class="m406-metric"><span>Текущий взнос</span><strong>${money(lastResult.sourcePlannedMonthly,'EUR')}</strong></div><div class="m406-metric"><span>Необходимый взнос</span><strong>${money(lastResult.requiredMonthlyContribution,'EUR')}</strong></div><div class="m406-metric"><span>Дополнительно в цель</span><strong>${money(lastResult.additionalMonthlyContribution,'EUR')}</strong></div><div class="m406-metric"><span>Финансирование в месяц</span><strong>${money(lastResult.requiredMonthlyFundingRelief,'EUR')}</strong></div></div><div class="m406-solver-split"><div class="m406-solver-callout"><strong>Информационная часть дохода</strong>${money(split.additionalIncome,'EUR')} в месяц<br><small>${esc(lastResult.incomeMeaning)}</small></div><div class="m406-solver-callout"><strong>Информационная часть экономии</strong>${money(split.expenseReduction,'EUR')} в месяц<br><small>${esc(lastResult.expenseMeaning)}</small></div></div><div class="m406-note">Цель: <strong>${esc(lastResult.goalName)}</strong>. Желаемая дата: <strong>${fmtDate(lastResult.targetDate)}</strong>. Прогнозный минимум после расчёта: <strong>${money(lastResult.resultingMinimumOperating,'EUR')}</strong>. Никакие реальные взносы не изменены.</div>${lastResult.feasibilityLimited?'<div class="m406-solver-limit">Расчёт взноса использует всю дату цели, а проверка доступных денег ограничена ближайшими 365 днями.</div>':''}</div>`}
    function render(){installDom();const section=$('m406Solver');if(!section)return;section.innerHTML=`<div class="m406-head"><div><h2>Решить задачу</h2><small>Рассчитать необходимое изменение без правки реального плана</small></div><span class="m406-badge">Информация</span></div><div class="m406-solver-tabs" style="margin-top:10px"><button type="button" data-solver-mode="deficit" class="${mode==='deficit'?'active':''}">Убрать дефицит</button><button type="button" data-solver-mode="goal" class="${mode==='goal'?'active':''}">Достичь цели к дате</button></div><div class="field"><label>Основа расчёта</label><select id="m406SolverSource">${sourceOptions()}</select></div>${mode==='deficit'?`<div class="field"><label>Горизонт</label><select id="m406SolverHorizon"><option value="30"${horizonDays===30?' selected':''}>30 дней</option><option value="90"${horizonDays===90?' selected':''}>90 дней</option></select></div>`:`<div class="m406-grid"><div class="field"><label>Накопление</label><select id="m406SolverGoal">${goalOptions()}</select></div><div class="field"><label>Достичь к дате</label><input id="m406SolverDate" type="date" value="${esc(defaultTarget())}"></div></div>`}<div class="field"><label>Как разделить расчёт</label><div class="m406-solver-range"><input id="m406SolverShare" type="range" min="0" max="100" step="5" value="${incomeShare}"><strong id="m406SolverShareText">Доход ${incomeShare}% · расходы ${100-incomeShare}%</strong></div></div><div class="m406-note">Дополнительный доход здесь означает размер недостающего финансирования. Программа не предполагает, что доход можно просто увеличить, и не создаёт плановый приход.</div><div class="sheet-actions"><button id="m406SolverCalculate" class="btn primary" type="button">Рассчитать</button></div><div id="m406SolverOutput">${renderResult()}</div>`;bind()}
    function bind(){const sourceInput=$('m406SolverSource'),share=$('m406SolverShare'),calculate=$('m406SolverCalculate');sourceInput.onchange=()=>{const value=sourceInput.value;if(value.startsWith('scenario:')){source='scenario';scenarioId=value.slice(9)}else{source='actual';scenarioId=''};lastResult=null;render()};const horizon=$('m406SolverHorizon');if(horizon)horizon.onchange=()=>{horizonDays=Number(horizon.value)===90?90:30;lastResult=null};const goal=$('m406SolverGoal');if(goal)goal.onchange=()=>{goalId=goal.value;lastResult=null};const date=$('m406SolverDate');if(date)date.onchange=()=>{targetDate=date.value;lastResult=null};share.oninput=()=>{incomeShare=Number(share.value);$('m406SolverShareText').textContent=`Доход ${incomeShare}% · расходы ${100-incomeShare}%`};calculate.onclick=calculateNow;for(const button of document.querySelectorAll('[data-solver-mode]'))button.onclick=()=>{mode=button.dataset.solverMode;lastResult=null;render()}}
    function calculateNow(){const input={source,scenarioId,incomeShare};lastResult=mode==='deficit'?solver.solveDeficit(state,{...input,horizonDays},deps,{from:now()}):solver.solveGoal(state,{...input,goalId,targetDate},deps,{from:now()});render()}
    const previous=runtime.getRenderAll();runtime.setRenderAll(function(){const result=previous();render();return result});
    if(new URLSearchParams(location.search).has('test')){window.__FP_TEST__=window.__FP_TEST__||{};window.__FP_TEST__.solver={deficit:input=>solver.solveDeficit(state,input,deps,{from:now()}),goal:input=>solver.solveGoal(state,input,deps,{from:now()}),open:()=>{runtime.showScreen('whatIf');render();return true},setMode:value=>{mode=value==='goal'?'goal':'deficit';lastResult=null;render();return mode},text:()=>document.getElementById('m406Solver')?.textContent||'',actualFingerprint:()=>whatIf.actualFingerprint(state),scenarioFingerprint:()=>JSON.stringify(state.whatIfScenarios||[])}}
    render();window.__FP_M4_06_SOLVER_READY__=true;
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wait,{once:true});else wait();
})();
