(()=>{'use strict';if(window.__FP_M4_03_BUDGET_UI_BOOTSTRAP__)return;window.__FP_M4_03_BUDGET_UI_BOOTSTRAP__=true;const end=Date.now()+30000,wait=()=>{const r=window.__FP_RUNTIME__,b=window.FamilyPilotBudgetDesigner,a=window.FamilyPilotSavingsAccounts,l=window.FamilyPilotSavingsGoals;if(!r||!b||!a||!l||window.__FP_M4_03_READY__!==true){if(Date.now()>=end){window.__FP_M4_03_BUDGET_UI_ERROR__='Budget designer dependencies did not become ready';return}setTimeout(wait,25);return}install(r,b,a,l)};function install(runtime,budget,accounts,legacy){if(window.__FP_M4_03_BUDGET_UI__)return;window.__FP_M4_03_BUDGET_UI__=true;const{state,scopeApi,$,esc,money,save,showScreen,toast,now}=runtime;let horizon=30;const capital=()=>Number(scopeApi.capitalSnapshot(state)?.capital)||0,fmt=v=>v?new Intl.DateTimeFormat('ru-RU',{day:'numeric',month:'short',year:'numeric'}).format(new Date(v)):'—',fmtMonth=v=>new Intl.DateTimeFormat('ru-RU',{month:'long',year:'numeric'}).format(new Date(v)),signed=v=>`${Number(v)<0?'−':''}${money(Math.abs(Number(v)||0),'EUR')}`,checked=n=>[...document.querySelectorAll(`input[name="${n}"]`)].filter(x=>x.checked).map(x=>x.value);const marker=document.createElement('meta');marker.name='familypilot-package';marker.content='m4-03-budget-designer-scenarios-v1';document.head.appendChild(marker);const style=document.createElement('style');style.textContent='.budget-stack{display:grid;gap:9px}.budget-summary{display:grid;grid-template-columns:repeat(2,1fr);gap:7px}.budget-metric,.budget-row{border:1px solid var(--line);border-radius:14px;background:var(--card2);padding:10px}.budget-metric span,.budget-metric small,.budget-row small{display:block;color:var(--muted);font-size:10px}.budget-metric strong{display:block;margin-top:4px;font-size:16px}.budget-negative{color:var(--red)!important}.budget-positive{color:var(--green)!important}.budget-head{display:flex;align-items:flex-start;justify-content:space-between;gap:8px}.budget-head h2{margin:0;font-size:15px}.budget-head small{display:block;color:var(--muted);font-size:10px;margin-top:3px}.budget-badge{display:inline-flex;border-radius:999px;padding:4px 8px;font-size:9px;font-weight:900;background:var(--card2);color:var(--muted)}.budget-badge.alert{color:var(--red)}.budget-badge.ok{color:var(--green)}.budget-note{border:1px solid var(--line);border-radius:13px;background:var(--card2);padding:9px;color:var(--muted);font-size:11px;line-height:1.45}.budget-scenario{border:1px solid var(--line);border-radius:15px;padding:11px}.budget-scenario.actionable{border-color:color-mix(in srgb,var(--green) 35%,var(--line))}.budget-scenario.info{border-color:color-mix(in srgb,var(--blue) 35%,var(--line))}.budget-scenario h3{margin:0 0 6px;font-size:14px}.budget-check{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:8px;align-items:start}.budget-check input{margin-top:3px}.budget-amount{font-weight:900}.budget-actions{display:flex;gap:6px;flex-wrap:wrap}.budget-actions .btn{min-height:34px;padding:6px 9px;font-size:10px}.budget-apply{width:100%;margin-top:8px}.budget-policy{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:8px;align-items:center}.budget-policy label{font-size:10px;color:var(--muted);display:flex;gap:5px;align-items:center}.budget-empty{border:1px dashed var(--line);border-radius:14px;padding:15px;text-align:center;color:var(--muted);font-size:12px}.budget-causes{display:grid;gap:5px;margin-top:6px}.budget-cause{display:flex;justify-content:space-between;gap:8px;font-size:11px}.budget-cause span{color:var(--muted)}.budget-tabs{display:flex;gap:4px}.budget-tabs button{border:1px solid var(--line);border-radius:9px;background:var(--card2);color:var(--muted);padding:6px 8px;font-size:10px;font-weight:850}.budget-tabs button.active{color:var(--green)}.budget-module-alert{color:var(--red)!important}.budget-game{font-size:10px;color:var(--muted);text-align:center;padding:5px 8px}@media(max-width:380px){.budget-head{display:grid}.budget-policy{grid-template-columns:1fr auto}.budget-policy label{grid-column:1/-1}.budget-check{grid-template-columns:auto minmax(0,1fr)}.budget-amount{grid-column:2}}';document.head.appendChild(style);
function installDom(){const plans=$('plansScreen');if(plans&&!$('budgetDesignerModule')){const anchor=$('planSavingsModule')||plans.querySelector('.plan-module'),button=document.createElement('button');button.id='budgetDesignerModule';button.type='button';button.className='plan-module';button.innerHTML='<span class="plan-module-copy"><strong>Проектирование бюджета</strong><small>Прогноз, дефицит и выравнивание накоплений</small></span><span id="budgetDesignerState" class="plan-module-state">Открыть</span>';anchor?.parentNode?anchor.parentNode.insertBefore(button,anchor):plans.appendChild(button)}if(!$('budgetDesignerScreen')){const s=document.createElement('section');s.id='budgetDesignerScreen';s.className='screen';s.innerHTML='<div class="page-title"><button class="back" data-budget-back="plans">‹</button><div class="page-title-copy"><h1>Проектирование бюджета</h1><small id="budgetPeriodLabel" class="scope-context"></small></div></div><section id="budgetSummaryCard" class="card section"></section><section id="budgetAttentionCard" class="card section"></section><section id="budgetDeficitCard" class="card section"></section><section id="budgetMonthEndCard" class="card section"></section><section id="budgetReserveCard" class="card section"></section><section id="budgetPolicyCard" class="card section"></section><div class="budget-game">Идея на развитие: спокойная игра в правильное распределение финансов — без баллов, давления и скрытых решений.</div>';$('moreScreen').parentNode.insertBefore(s,$('moreScreen'))}}
function summary(){const f=budget.forecast(state,accounts,{baseCapital:capital(),from:now(),horizonDays:horizon});$('budgetPeriodLabel').textContent=`${horizon} дней · ${fmtMonth(now())}`;$('budgetSummaryCard').innerHTML=`<div class="budget-head"><div><h2>План месяца</h2><small>Фактическое состояние, обязательства, доходы и накопления.</small></div><div class="budget-tabs"><button data-budget-horizon="30" class="${horizon===30?'active':''}">30 дней</button><button data-budget-horizon="90" class="${horizon===90?'active':''}">90 дней</button></div></div><div class="budget-summary" style="margin-top:10px"><div class="budget-metric"><span>Доступно сейчас</span><strong>${signed(f.initial.operating)}</strong></div><div class="budget-metric"><span>Остаток в конце</span><strong class="${f.endingOperating<0?'budget-negative':''}">${signed(f.endingOperating)}</strong></div><div class="budget-metric"><span>Минимум</span><strong class="${f.minimumOperating<0?'budget-negative':'budget-positive'}">${signed(f.minimumOperating)}</strong><small>${fmt(f.minimumOperatingDate)}</small></div><div class="budget-metric"><span>Общий капитал</span><strong>${money(f.endingTotalCapital,'EUR')}</strong></div></div>`}
function attention(){const r=budget.ensureMonthlyRecalculation(state,accounts,now()),pending=r.changes.filter(x=>!x.confirmedAt);$('budgetAttentionCard').innerHTML=`<div class="budget-head"><div><h2>Изменения планов в начале месяца</h2><small>Расчёт обновляется автоматически. Каждый изменившийся пункт подтверждается отдельно.</small></div><span class="budget-badge ${pending.length?'alert':'ok'}">${pending.length?`${pending.length} проверить`:'Подтверждено'}</span></div><div class="budget-stack" style="margin-top:10px">${r.changes.length?r.changes.map(x=>`<div class="budget-row"><div class="budget-head"><div><strong>${esc(x.title)}</strong><small>${x.planningMode==='fixed_date'?`Взнос: ${money(x.beforeRequiredMonthly,'EUR')} → ${money(x.afterRequiredMonthly,'EUR')}`:`Срок: ${fmt(x.beforeExpectedCompletionDate)} → ${fmt(x.afterExpectedCompletionDate)}`}</small></div><span class="budget-badge ${x.confirmedAt?'ok':'alert'}">${x.confirmedAt?'Подтверждено':'Внимание'}</span></div><div class="budget-actions" style="margin-top:7px">${x.confirmedAt?'':`<button class="btn primary" data-budget-confirm-recalc="${esc(x.id)}" data-budget-month="${esc(r.monthKey)}">Подтвердить этот пункт</button>`}<button class="btn secondary" data-budget-edit-goal="${esc(x.goalId)}">Изменить сумму или срок</button></div></div>`).join(''):'<div class="budget-empty">В этом месяце суммы и сроки не изменились.</div>'}</div>`}
const checks=(s,name)=>s.items.length?s.items.map(x=>`<label class="budget-row budget-check"><input type="checkbox" name="${name}" value="${esc(x.id)}"><span><strong>${esc(x.title)}</strong><small>${s.id==='reduce_contributions'?`${x.monthKey}: ${money(x.originalAmount,'EUR')} → ${money(x.adjustedAmount,'EUR')}`:`Вернуть в основные деньги. Приоритет ${x.priority}`}</small></span><span class="budget-amount">${s.id==='reduce_contributions'?`−${money(x.reduction,'EUR')}`:money(x.amount,'EUR')}</span></label>`).join(''):'<div class="budget-empty">Сейчас этот способ не даёт эффекта.</div>';
function deficit(){const a=budget.deficitAnalysis(state,accounts,{baseCapital:capital(),from:now(),horizonDays:horizon});if(!a.deficit){$('budgetDeficitCard').innerHTML='<div class="budget-head"><div><h2>Дефицит</h2><small>Отрицательный остаток не прогнозируется.</small></div><span class="budget-badge ok">План сходится</span></div>';return}const s1=a.scenarios.find(x=>x.id==='reduce_contributions'),s2=a.scenarios.find(x=>x.id==='return_from_savings'),s3=a.scenarios.find(x=>x.id==='income_gap'),s4=a.scenarios.find(x=>x.id==='combined_information');$('budgetDeficitCard').innerHTML=`<div class="budget-head"><div><h2>Нехватка ${money(a.amount,'EUR')}</h2><small>Критическая дата: ${fmt(a.criticalDate)}. Автоматических изменений нет.</small></div><span class="budget-badge alert">Требует решения</span></div><div class="budget-note" style="margin-top:10px"><strong>Причины</strong><div class="budget-causes">${a.causes.map(x=>`<div class="budget-cause"><span>${esc(x.title)} · ${fmt(x.date)}</span><strong>${money(x.amount,'EUR')}</strong></div>`).join('')}</div></div><div class="budget-stack" style="margin-top:10px"><div class="budget-scenario actionable"><h3>1. Уменьшить будущие взносы</h3><div class="budget-note">Только будущие плановые отчисления. Каждый пункт подтверждается отдельной галочкой.</div><div class="budget-stack" style="margin-top:8px">${checks(s1,'budget-reduce')}</div><button class="btn primary budget-apply" data-budget-apply-reduce>Применить подтверждённые пункты</button><small>Облегчение ${money(s1.relief,'EUR')}; остаточная дыра ${money(s1.remainingGap,'EUR')}.</small></div><div class="budget-scenario actionable"><h3>2. Временно вернуть часть накоплений</h3><div class="budget-note">Внутренний перевод: капитал не уменьшается. Каждый перевод подтверждается отдельно.</div><div class="budget-stack" style="margin-top:8px">${checks(s2,'budget-return')}</div><button class="btn primary budget-apply" data-budget-apply-return>Применить подтверждённые переводы</button></div><div class="budget-scenario info"><h3>3. Необходимый дополнительный доход</h3><div class="budget-note">До ${fmt(s3.byDate)} не хватает ${money(s3.amount,'EUR')}. Это показатель дыры, а не обещание, что доход можно просто увеличить.</div></div><div class="budget-scenario info"><h3>4. Комбинированный ориентир</h3><div class="budget-note">Уменьшение взносов ${money(s4.reduction,'EUR')}, возврат ${money(s4.returned,'EUR')}, оставшаяся потребность в доходе ${money(s4.incomeGap,'EUR')}. Только информация.</div></div></div>`}
function monthEnd(){const r=budget.monthEndReview(state,accounts,{baseCapital:capital(),asOf:now()});$('budgetMonthEndCard').innerHTML=`<div class="budget-head"><div><h2>Выравнивание накоплений за ${fmtMonth(r.periodStart)}</h2><small>Остаток можно направить на пропущенные плановые отчисления.</small></div><span class="budget-badge">Остаток ${money(r.operatingRemainder,'EUR')}</span></div><div class="budget-stack" style="margin-top:10px">${r.proposals.length?r.proposals.map(x=>`<label class="budget-row budget-check"><input type="checkbox" name="budget-catchup" value="${esc(x.id)}"><span><strong>${esc(x.title)}</strong><small>План ${money(x.expected,'EUR')}, внесено ${money(x.actual,'EUR')}, пропущено ${money(x.shortfall,'EUR')}</small></span><span class="budget-amount">+${money(x.amount,'EUR')}</span></label>`).join(''):'<div class="budget-empty">Нет одновременно свободного остатка и пропущенных накоплений.</div>'}</div>${r.proposals.length?'<button class="btn primary budget-apply" data-budget-apply-catchup>Разнести подтверждённые суммы</button><div class="budget-note" style="margin-top:8px">Неподтверждённые суммы остаются в основных деньгах.</div>':''}`}
function reserve(){const p=budget.reserveProposal(state,accounts,{baseCapital:capital(),asOf:now()}),existing=(state.savingsGoals||[]).find(x=>x.id===state.reserveSavingsGoalId&&x.status==='active');$('budgetReserveCard').innerHTML=`<div class="budget-head"><div><h2>Резерв на покрытие дефицита</h2><small>Отдельное накопление для временной нехватки или ускорения другой цели.</small></div><span class="budget-badge ${existing?'ok':''}">${existing?'Создан':'Проект'}</span></div>${existing?`<div class="budget-note" style="margin-top:10px">«${esc(existing.name)}»: ${money(existing.savedAmount,'EUR')}.</div>`:`<div class="budget-summary" style="margin-top:10px"><div class="budget-metric"><span>Цель</span><strong>${money(p.targetAmount,'EUR')}</strong></div><div class="budget-metric"><span>Мягкий взнос</span><strong>${money(p.monthlyContribution,'EUR')}/мес.</strong><small>${p.monthsToTarget?`около ${p.monthsToTarget} мес.`:'нужна настройка'}</small></div></div><div class="budget-note" style="margin-top:8px">Первый проект берёт 5% от незамороженных взносов. Капитал не уменьшается, меняется распределение.</div><div class="budget-stack" style="margin-top:8px"><label class="budget-row budget-check"><input type="checkbox" name="budget-reserve" value="${esc(p.createItemId)}"><span><strong>Создать резервное накопление</strong><small>Цель ${money(p.targetAmount,'EUR')}</small></span><span class="budget-amount">Отдельно</span></label>${p.sourceItems.map(x=>`<label class="budget-row budget-check"><input type="checkbox" name="budget-reserve" value="${esc(x.id)}"><span><strong>${esc(x.title)}</strong><small>${money(x.currentMonthly,'EUR')} → ${money(x.adjustedMonthly,'EUR')} в следующем месяце</small></span><span class="budget-amount">${money(x.diversion,'EUR')}</span></label>`).join('')}</div><button class="btn primary budget-apply" data-budget-apply-reserve>Создать по подтверждённым пунктам</button>`}`}
function policies(){const goals=(state.savingsGoals||[]).filter(x=>x.status==='active');$('budgetPolicyCard').innerHTML=`<div class="budget-head"><div><h2>Правила сценариев</h2><small>Низкий приоритет корректируется раньше. Замороженная цель не участвует.</small></div></div><div class="budget-stack" style="margin-top:10px">${goals.length?goals.map(g=>{const p=budget.policyFor(state,g.id);return `<div class="budget-row budget-policy"><div><strong>${esc(g.name)}</strong><small>1 — низкий, 5 — высокий</small></div><select data-budget-priority="${esc(g.id)}">${[1,2,3,4,5].map(v=>`<option value="${v}"${p.priority===v?' selected':''}>${v}</option>`).join('')}</select><label><input type="checkbox" data-budget-frozen="${esc(g.id)}"${p.frozen?' checked':''}> Заморозить</label></div>`}).join(''):'<div class="budget-empty">Сначала создайте накопления.</div>'}</div>`}
function render(){installDom();budget.normalizeState(state,now());summary();attention();deficit();monthEnd();reserve();policies();const pending=budget.pendingAttentionCount(state,accounts,now()),a=budget.deficitAnalysis(state,accounts,{baseCapital:capital(),from:now(),horizonDays:30}),node=$('budgetDesignerState');if(node){node.textContent=a.deficit?`−${money(a.amount,'EUR')}`:pending?`${pending} проверить`:'Открыть';node.classList.toggle('budget-module-alert',a.deficit||pending)}}
function applyReduce(){const a=budget.deficitAnalysis(state,accounts,{baseCapital:capital(),from:now(),horizonDays:horizon}),s=a.scenarios.find(x=>x.id==='reduce_contributions'),ids=checked('budget-reduce');if(!ids.length){toast('Подтвердите хотя бы один пункт');return}budget.applyContributionScenario(state,s.items,ids,state.currentMemberId,now());save();runtime.renderAll();toast('Подтверждённые изменения применены')}
function applyReturn(){const a=budget.deficitAnalysis(state,accounts,{baseCapital:capital(),from:now(),horizonDays:horizon}),s=a.scenarios.find(x=>x.id==='return_from_savings'),ids=checked('budget-return');if(!ids.length){toast('Подтвердите хотя бы один перевод');return}const r=budget.applyReturnScenario(state,accounts,s.items,ids,capital(),state.currentMemberId,now());if(!r.ok){toast(r.errors[0]?.error||'Не удалось выполнить перевод');return}save();runtime.renderAll();toast('Подтверждённые переводы выполнены')}
function applyCatchup(){const review=budget.monthEndReview(state,accounts,{baseCapital:capital(),asOf:now()}),ids=checked('budget-catchup');if(!ids.length){toast('Подтвердите хотя бы один пункт');return}const r=budget.applyMonthEndAllocation(state,accounts,review,ids,capital(),state.currentMemberId,now());if(!r.ok){toast(r.errors[0]?.error||'Не удалось распределить остаток');return}save();runtime.renderAll();toast('Остаток распределён')}
function applyReserve(){const p=budget.reserveProposal(state,accounts,{baseCapital:capital(),asOf:now()}),r=budget.applyReserveProposal(state,accounts,legacy,p,checked('budget-reserve'),state.currentMemberId,now());if(!r.ok){toast(r.error);return}save();runtime.renderAll();toast('Резервное накопление создано')}
function editGoal(goalId){showScreen('savingsGoals');runtime.renderAll();setTimeout(()=>{const button=[...document.querySelectorAll('[data-m403-plan]')].find(x=>x.dataset.m403Plan===goalId);button?button.click():toast('Откройте план нужного накопления')},0)}
const baseRender=runtime.getRenderAll();runtime.setRenderAll(function(){const r=baseRender();render();return r});installDom();document.addEventListener('click',e=>{const open=e.target.closest('#budgetDesignerModule'),back=e.target.closest('[data-budget-back]'),range=e.target.closest('[data-budget-horizon]'),confirm=e.target.closest('[data-budget-confirm-recalc]'),edit=e.target.closest('[data-budget-edit-goal]'),r1=e.target.closest('[data-budget-apply-reduce]'),r2=e.target.closest('[data-budget-apply-return]'),r3=e.target.closest('[data-budget-apply-catchup]'),r4=e.target.closest('[data-budget-apply-reserve]');if(!(open||back||range||confirm||edit||r1||r2||r3||r4))return;e.preventDefault();e.stopImmediatePropagation();if(open){showScreen('budgetDesigner');runtime.renderAll()}else if(back){showScreen(back.dataset.budgetBack);runtime.renderAll()}else if(range){horizon=Number(range.dataset.budgetHorizon)||30;render()}else if(confirm){budget.confirmMonthlyRecalculationItem(state,confirm.dataset.budgetMonth,confirm.dataset.budgetConfirmRecalc,state.currentMemberId,now());save();runtime.renderAll();toast('Пункт подтверждён')}else if(edit)editGoal(edit.dataset.budgetEditGoal);else if(r1)applyReduce();else if(r2)applyReturn();else if(r3)applyCatchup();else if(r4)applyReserve()},true);document.addEventListener('change',e=>{const p=e.target.closest('[data-budget-priority]'),f=e.target.closest('[data-budget-frozen]');if(!(p||f))return;const goalId=(p||f).dataset.budgetPriority||(p||f).dataset.budgetFrozen,current=budget.policyFor(state,goalId);budget.setPolicy(state,goalId,{priority:p?Number(p.value):current.priority,frozen:f?f.checked:current.frozen},state.currentMemberId,now());save();runtime.renderAll()},true);
if(new URLSearchParams(location.search).has('test')){const test=window.__FP_TEST__=window.__FP_TEST__||{};test.budget={open:()=>{showScreen('budgetDesigner');runtime.renderAll()},analysis:(d=30)=>budget.deficitAnalysis(state,accounts,{baseCapital:capital(),from:now(),horizonDays:d}),monthReview:()=>budget.monthEndReview(state,accounts,{baseCapital:capital(),asOf:now()}),recalculation:()=>budget.ensureMonthlyRecalculation(state,accounts,now()),reserve:()=>budget.reserveProposal(state,accounts,{baseCapital:capital(),asOf:now()}),setPolicy:(g,i)=>{const r=budget.setPolicy(state,g,i,state.currentMemberId,now());save();runtime.renderAll();return r},applyReduce:(items,ids)=>{const r=budget.applyContributionScenario(state,items,ids,state.currentMemberId,now());save();runtime.renderAll();return r},applyReturn:(items,ids)=>{const r=budget.applyReturnScenario(state,accounts,items,ids,capital(),state.currentMemberId,now());save();runtime.renderAll();return r},applyCatchup:(review,ids)=>{const r=budget.applyMonthEndAllocation(state,accounts,review,ids,capital(),state.currentMemberId,now());save();runtime.renderAll();return r},applyReserve:(p,ids)=>{const r=budget.applyReserveProposal(state,accounts,legacy,p,ids,state.currentMemberId,now());save();runtime.renderAll();return r},confirmRecalc:(r,item)=>{const x=budget.confirmMonthlyRecalculationItem(state,r.monthKey,item,state.currentMemberId,now());save();runtime.renderAll();return x},seedDeficit:(amount,date=now()+7*DAY)=>{const k=Date.now(),ruleId=`budget-test-rule-${k}`,occurrenceId=`budget-test-occ-${k}`;state.obligationRules=state.obligationRules||[];state.obligationOccurrences=state.obligationOccurrences||[];state.obligationRules.push({id:ruleId,name:'Тестовый крупный платёж',expectedAmount:amount,status:'active'});state.obligationOccurrences.push({id:occurrenceId,ruleId,dueAt:date,expectedAmount:amount,status:'scheduled'});save();runtime.renderAll();return{ruleId,occurrenceId}},state:()=>JSON.parse(JSON.stringify({policies:state.savingsGoalPolicies,overrides:state.savingsContributionOverrides,reviews:state.savingsMonthlyPlanReviews,reserveGoalId:state.reserveSavingsGoalId,reserveDesigns:state.savingsReserveDesigns}))}}
budget.normalizeState(state,now());budget.ensureMonthlyRecalculation(state,accounts,now());save();runtime.renderAll();window.__FP_M4_03_BUDGET_READY__=true}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wait,{once:true});else wait()})();