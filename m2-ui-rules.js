(function(){
  function selectedAction(){return document.querySelector('.action.active')?.dataset.action||'borrow'}
  function sync(){const input=document.getElementById('extra');if(!input)return;const field=input.closest('label');if(!field)return;const repayment=['repay','receive'].includes(selectedAction());field.classList.toggle('hidden',!repayment);if(!repayment)input.value='';const label=field.querySelector('.label-row > span');if(label)label.textContent='Дополнительно сверх основного долга';input.placeholder='Например, проценты или подарок';let helper=field.querySelector('[data-extra-helper]');if(!helper){helper=document.createElement('span');helper.className='tiny';helper.dataset.extraHelper='true';field.appendChild(helper)}helper.textContent='Необязательно. Эта сумма не изменяет основной долг.'}
  function help(){const b=document.querySelector('[data-help="extra"]');if(!b)return;b.onclick=()=>{document.getElementById('helpTitle').textContent='Дополнительно сверх основного долга';document.getElementById('helpText').textContent='Поле показывается только при возврате долга. Используйте его для процентов или подарка. Эта сумма сохраняется отдельно и не меняет основной остаток долга.';document.getElementById('helpModal').classList.add('open')}}
  function install(){sync();help();document.getElementById('actions')?.addEventListener('click',()=>setTimeout(sync,0));document.getElementById('saveDebtAction')?.addEventListener('click',()=>{if(!['repay','receive'].includes(selectedAction())){const i=document.getElementById('extra');if(i)i.value=''}},true)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();