(function(){
  function selectedAction(){return document.querySelector('.action.active')?.dataset.action||'borrow'}
  function syncExtraField(){const input=document.getElementById('extra');if(!input)return;const field=input.closest('label');if(!field)return;const isRepayment=['repay','receive'].includes(selectedAction());field.classList.toggle('hidden',!isRepayment);if(!isRepayment)input.value='';const label=field.querySelector('.label-row > span');if(label)label.textContent='Дополнительно сверх основного долга';input.placeholder='Например, проценты или подарок';let helper=field.querySelector('[data-extra-helper]');if(!helper){helper=document.createElement('span');helper.className='tiny';helper.dataset.extraHelper='true';field.appendChild(helper)}helper.textContent='Необязательно. Эта сумма не изменяет основной долг.'}
  const originalEventMeta=eventMeta;
  eventMeta=function(event){if(event.kind==='offset')return{icon:'⇄',verb:'Взаимозачёт встречных долгов'};return originalEventMeta(event)};
  function roundMoney(value){return Math.round((Number(value)||0)*100)/100}
  function offsetParty(party,date,description){if(!party)return 0;const owedByMe=roundMoney(party.owedByMe),owedToMe=roundMoney(party.owedToMe),amount=roundMoney(Math.min(owedByMe,owedToMe));if(!(amount>0))return 0;party.owedByMe=roundMoney(owedByMe-amount);party.owedToMe=roundMoney(owedToMe-amount);recordEvent({kind:'offset',partyId:party.id,principal:amount,date:date||localToday(),description:description||'Автоматический взаимозачёт встречных долгов'});return amount}
  function offsetAll(date,description){let total=0;state.parties.forEach(party=>{total=roundMoney(total+offsetParty(party,date,description))});if(total>0){save();render()}return total}
  renderBalance=function(){const party=selectedParty();if(!party){document.getElementById('balanceBox').innerHTML='<div class="balance-main balance-zero">Контрагент не выбран</div><div class="tiny">Создайте человека или организацию, чтобы связать долговое действие.</div>';return}const owedByMe=roundMoney(party.owedByMe),owedToMe=roundMoney(party.owedToMe);let result='<div class="tiny">Итог взаиморасчётов с '+esc(party.name)+'</div>';if(owedByMe>0)result+='<div class="balance-main owed">Я должен '+money(owedByMe)+'</div>';else if(owedToMe>0)result+='<div class="balance-main receivable">Мне должны '+money(owedToMe)+'</div>';else result+='<div class="balance-main balance-zero">Расчёты закрыты</div>';result+='<div class="tiny">Встречные долги автоматически взаимозачитываются. История операций сохраняется.</div>';document.getElementById('balanceBox').innerHTML=result};
  renderPartyList=function(){const container=document.getElementById('partyList');if(!state.parties.length){container.innerHTML='<div class="notice">Контрагентов пока нет.</div>';return}container.innerHTML=state.parties.map(party=>{const owedByMe=roundMoney(party.owedByMe),owedToMe=roundMoney(party.owedToMe);const result=owedByMe>0?'<span class="pill">Итог: я должен '+money(owedByMe)+'</span>':owedToMe>0?'<span class="pill">Итог: мне должны '+money(owedToMe)+'</span>':'<span class="pill">Расчёты закрыты</span>';return'<div class="party-card '+(party.id===state.selectedPartyId?'active':'')+'" data-party="'+party.id+'"><div class="party-name">'+esc(party.name)+'</div><div class="party-meta">'+(party.type==='organization'?'Организация':'Человек')+'</div><div class="balances">'+result+'</div></div>'}).join('')};
  const originalApplyNormalAction=applyNormalAction;
  applyNormalAction=function(data){const party=partyById(data.partyId);originalApplyNormalAction(data);const offset=offsetParty(party,data.date,'Автоматический взаимозачёт после долговой операции');if(offset>0){save();render();toast('Встречные долги взаимозачтены на '+money(offset))}};
  function wrapButton(id,description){const button=document.getElementById(id);if(!button||typeof button.onclick!=='function')return;const original=button.onclick;button.onclick=function(event){original.call(this,event);setTimeout(()=>{const total=offsetAll(document.getElementById('date')?.value||localToday(),description);if(total>0)toast('Встречные долги взаимозачтены на '+money(total))},0)}}
  function install(){syncExtraField();const helpButton=document.querySelector('[data-help="extra"]');if(helpButton)helpButton.onclick=()=>{document.getElementById('helpTitle').textContent='Дополнительно сверх основного долга';document.getElementById('helpText').textContent='Поле показывается только при возврате долга. Используйте его для процентов или подарка. Эта сумма сохраняется отдельно и не меняет основной остаток долга.';document.getElementById('helpModal').classList.add('open')};document.getElementById('actions')?.addEventListener('click',()=>setTimeout(syncExtraField,0));document.getElementById('saveDebtAction')?.addEventListener('click',()=>{if(!['repay','receive'].includes(selectedAction())){const input=document.getElementById('extra');if(input)input.value=''}},true);wrapButton('saveOpening','Автоматический взаимозачёт после добавления стартового долга');wrapButton('resolveCounterDebt','Автоматический взаимозачёт после создания встречного долга');const migrated=offsetAll(localToday(),'Автоматический взаимозачёт существовавших встречных долгов после обновления правила');if(migrated>0)toast('Встречные долги взаимозачтены на '+money(migrated));else render()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();